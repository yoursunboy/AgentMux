# Project Boundary Rules

- The actual AgentMux repository is:
  `D:\AI\Projects\2026 AgentMux\AgentMux`
- The parent folder:
  `D:\AI\Projects\2026 AgentMux`
  is an organizational collection and may contain non-code materials.
- Do not modify `Reference`, `Design`, `Notes`, `Archive`, or sibling folders unless explicitly requested.
- Git should be initialized inside the actual Project directory, not the Collection directory.
- Claude should be launched from the registered Project directory, not the Collection directory.
- Avoid placing `CLAUDE.md` in Collection directories unless inheritance is intentional.
- AgentMux runtime metadata belongs in AgentMux data storage, not inside managed repositories.
