# Local Claude settings
.claude/settings.local.json

# Local/runtime data
data/
runtime/
*.db
*.db-shm
*.db-wal

# Frontend
web/node_modules/
web/dist/
*.tsbuildinfo

# Go
bin/
dist/

# Environment
.env
.env.*
!.env.example

# Host credentials. Never committed: this file names machines and carries their
# passwords in the clear. It is listed here so that a `git add -A` cannot pick it
# up by accident, which is the only way it would ever reach a commit.
docs/serverinfo.md
*serverinfo*.local.md

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
