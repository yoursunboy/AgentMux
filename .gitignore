# Local Claude settings
.claude/settings.local.json

# Local/runtime data
data/
runtime/
*.db
*.db-shm
*.db-wal

# Frontend
web/node_modules/
web/dist/
*.tsbuildinfo

# Go
bin/
dist/

# Environment
.env
.env.*
!.env.example

# Host notes. Never committed: this file names machines and the experiments run
# on them, which is local operational detail rather than project source. It is
# listed here so that a `git add -A` cannot pick it up by accident, which is the
# only way it would ever reach a commit. No credential belongs in it - the file
# points at interactive authentication instead.
docs/serverinfo.md
*serverinfo*.local.md

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# The end-to-end fixture description, written by web/e2e/run.mjs for one run
# and true only of the machine that wrote it.
web/e2e/fixtures.json
