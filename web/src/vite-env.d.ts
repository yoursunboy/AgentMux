/// <reference types="vite/client" />

// Ambient types for Vite: `import './styles/global.css'` and import.meta.env.
// The tsconfig lists `types` explicitly, so this reference is what pulls
// vite/client in rather than it being picked up automatically.
